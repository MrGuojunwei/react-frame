import BaseForm from '../BaseForm';
import { Checkout } from 'antd';

class CheckoutForm extends BaseForm {
  getComponent() {
    const {

    } = this.props;
  }
}

CheckoutForm.propTypes = {

}

CheckoutForm.defaultProps = {

}

export default CheckoutForm;